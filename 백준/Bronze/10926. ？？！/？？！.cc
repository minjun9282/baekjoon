#include <iostream>
#include <string>
using namespace std;

int main(){
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    string id;
    cin >> id;
    cout << id << "?\?!" << '\n';

    return 0;
}